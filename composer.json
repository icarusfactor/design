{
    "name": "roundcube/desgin",
    "type": "roundcube-plugin",
    "description": "Plugin adds (Design) in taskbar. Used to make HTML emails from templates. ",
    "license": "GPL-3.0-or-later",
    "authors": [
        {
            "name": "Daniel Yount",
            "email": "factor@userspace.org",
            "role": "Lead"
        }
    ],
    "repositories": [
        {
            "type": "composer",
            "url": "https://plugins.roundcube.net"
        }
    ],
    "require": {
        "php": ">=7.4.0",
        "roundcube/plugin-installer": ">=0.1.3"
    },
    "config": {
        "allow-plugins": {
            "roundcube/plugin-installer": true
        }
    }
}
